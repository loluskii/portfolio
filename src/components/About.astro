
<section class="about" id="about">
  <div class="shell about-inner">
    <div class="about-grid">
      <div class="about-left" data-reveal>
        <span class="section-kicker">About</span>
        <h2 class="about-title">
          Designing impactful user interfaces for the past 6 years.
        </h2>
        <div class="about-links">
          <a href="/cv.pdf" target="_blank" rel="noreferrer" class="about-link" data-cursor-label="Open" data-cursor-tone="soft">Download CV ↗</a>
          <a href="https://linkedin.com/in/adeloreisaac" target="_blank" rel="noreferrer" class="about-link" data-cursor-label="Open" data-cursor-tone="soft">Connect on LinkedIn ↗</a>
        </div>
      </div>

      <div class="about-right" data-reveal>
        <p class="about-copy">
          I thrive where product strategy meets engineering reality. Anyone can design a perfect mockup, but my best work lives in the messy gap between intent and execution. I specialize in building calm, predictable user interfaces that shield users from backend chaos, shifting business rules, and technical constraints.
        </p>

        <p class="about-copy">
          Whether navigating real-time data in gaming, rigid legal workflows in compliance, or heavy transaction states in fintech, I design for how systems actually behave under the hood. I treat engineering limitations not as roadblocks, but as the boundaries that define great design. By collaborating closely with development teams, I ensure that the final, shipped product maintains its visual and functional integrity, transforming complex backend data into intuitive, high-performance digital experiences.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding: 120px 0 140px;
  }

  .about-grid {
    display: grid;
    grid-template-columns: minmax(0, 460px) minmax(0, 1fr);
    gap: 64px;
    align-items: start;
  }

  .about-left {
    display: grid;
    gap: 24px;
  }

  .section-kicker {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-muted);
  }

  .about-title {
    font-family: var(--font-display);
    font-size: 58px;
    font-weight: 600;
    line-height: 1.08;
  }

  .about-links {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
    padding-top: 24px;
  }

  .about-link {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text-secondary);
    transition: color 0.2s ease;
  }

  .about-link:hover,
  .about-link:focus-visible {
    color: var(--text-primary);
  }

  .about-right {
    display: grid;
    gap: 28px;
  }

  .about-copy {
    font-size: 18px;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 620px;
  }

  .experience-list {
    display: flex;
    flex-direction: column;
    border-top: 1px solid var(--line);
  }

  .experience-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto;
    gap: 18px;
    align-items: center;
    padding: 18px 0;
    border-bottom: 1px solid var(--line);
    font-size: 14px;
  }

  .experience-company {
    color: var(--text-primary);
    font-weight: 500;
  }

  .experience-role {
    color: var(--text-secondary);
  }

  .experience-year {
    color: var(--text-muted);
    white-space: nowrap;
  }

  @media (max-width: 960px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .about-title {
      font-size: 46px;
    }
  }

  @media (max-width: 640px) {
    .about {
      padding: 88px 0 104px;
    }

    .about-title {
      font-size: 36px;
    }

    .experience-row {
      grid-template-columns: 1fr;
      gap: 6px;
    }
  }
</style>
